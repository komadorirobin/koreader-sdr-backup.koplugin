local _ = require("gettext")

return {
    name = "sdrbackup",
    fullname = _("SDR Backup"),
    description = _("Back up and restore KOReader reading state over the local network"),
    version = "1.1.3",
}
